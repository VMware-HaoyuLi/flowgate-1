comment:  on # enable pull request comment

codecov:
  require_ci_to_pass: false


coverage:
  range: 70..100
  round: down
  precision: 2
  status:
    project:
      default:
        target: 100%
        threshold: 0%
        base: auto
    patch:
      default:
        target: 100%