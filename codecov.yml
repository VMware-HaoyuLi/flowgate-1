codecov:
  require_ci_to_pass: false

coverage:
  precision: 2
  round: down
  range: "10...100"
  status:
    project:
      default: false
      aggregator:
        target: 20%
        threshold: 0%
        flags:
          - aggregator
        base: parent
        paths:
          - "aggregator"
        if_not_found: success
        if_ci_failed: error
        informational: false
        only_pulls: true
      infoblox:
        target: 20%
        threshold: 0%
        flags:
          - infoblox
        base: parent
        paths:
          - "infoblox-worker"
        if_not_found: success
        if_ci_failed: error
        informational: false
        only_pulls: true
    patch: off

flags:
  aggregator:
    paths:
      - aggregator/
    carryforward: true
  infoblox:
    paths:
      - infoblox-worker/
    carryforward: true

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: true
  require_base: yes
  require_head: yes
  branches: master