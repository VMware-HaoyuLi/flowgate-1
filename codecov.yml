codecov:
  require_ci_to_pass: false

coverage:
  precision: 2
  round: down
  range: "10...100"

  status:
    project:
      default:
        threshold: 0%
    patch: off
    changes: no

comment:
  layout: "reach, diff, flags, files"
  behavior: default
  require_changes: no